{
  "version": 3,
  "configurePresets": [
    {
      "name": "default",
      "displayName": "Default",
      "description": "Default build using system compiler",
      "binaryDir": "${sourceDir}/build"
    }
  ]
}
